pipeline {
    agent {
        docker {
            image 'mcr.microsoft.com/dotnet/sdk:9.0'
            args '-u root:root'
        }
    }

    stages {
        stage('Restore') {
            steps {
                sh 'dotnet restore RoomReservationSystem-copy/RoomReservationSystem.sln'
            }
        }

        stage('Build') {
            steps {
                sh 'dotnet build RoomReservationSystem-copy/RoomReservationSystem.sln --configuration Release'
            }
        }

        stage('Run') {
            steps {
                sh 'dotnet run --project RoomReservationSystem-copy/RoomReservationSystem/RoomReservationSystem.csproj'
            }
        }
    }
}
